% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probit_monbart.R
\name{probit_monbart}
\alias{probit_monbart}
\title{Fit a probit monotone BART model}
\usage{
probit_monbart(
  x.train,
  y.train,
  x.test = NULL,
  sigest = NA,
  sigdf = 3,
  sigquant = 0.9,
  k = 2,
  power = 0.8,
  base = 0.25,
  sigmaf = NA,
  lambda = NA,
  offset = NULL,
  ntree = 200,
  ndpost = 1000,
  nskip = 100,
  mgsize = 50,
  nkeeptrain = ndpost,
  nkeeptest = ndpost,
  nkeeptestmean = ndpost,
  nkeeptreedraws = ndpost,
  printevery = 0
)
}
\arguments{
\item{x.train}{Numeric matrix or data frame of training predictors. Rows are
observations and columns are predictors.}

\item{y.train}{Binary response vector. Values must be \code{0} or \code{1}.}

\item{x.test}{Optional numeric matrix or data frame of predictors at which to
save posterior predictions. It must have the same columns, in the same
order, as \code{x.train}.}

\item{sigest, sigdf, sigquant, sigmaf, lambda}{Legacy compatibility arguments.
They are currently ignored because the latent probit variance is fixed at
one.}

\item{k}{Prior shrinkage parameter. Larger values shrink individual trees
more strongly.}

\item{power, base}{Parameters controlling the tree-depth prior.}

\item{offset}{Optional probit-scale intercept. By default it is
\code{qnorm(mean(y.train))}.}

\item{ntree}{Number of trees in the ensemble.}

\item{ndpost}{Number of posterior draws after burn-in.}

\item{nskip}{Number of burn-in iterations.}

\item{mgsize}{Number of support points in the discrete leaf-parameter prior.}

\item{nkeeptrain}{Number of posterior training draws to retain. Use zero to
retain none. The value must divide \code{ndpost}.}

\item{nkeeptest}{Number of posterior test draws to retain. Use zero to retain
none. The value must divide \code{ndpost}.}

\item{nkeeptestmean}{Number of test iterations used to calculate the native
posterior mean. Use zero to disable it. The value must divide \code{ndpost}.}

\item{nkeeptreedraws}{Number of serialized tree draws to retain. Use zero to
retain none. The value must divide \code{ndpost}.}

\item{printevery}{Print sampler progress every this many iterations. Use zero
(the default) for quiet operation.}
}
\value{
An object of class \code{"pmBART_fit"} containing posterior latent-scale
fits and probabilities. The most commonly used components are:
\describe{
\item{prob.train}{Matrix of retained training probability draws.}
\item{prob.train.mean}{Posterior mean training probabilities.}
\item{prob.test}{Matrix of retained test probability draws.}
\item{prob.test.mean}{Posterior mean test probabilities.}
\item{yhat.train, yhat.test}{Corresponding latent probit-scale draws.}
\item{treedraws}{Serialized trees and predictor cutpoints.}
}
}
\description{
Fits a Bayesian additive regression tree model for a binary outcome. Every
predictor is constrained to have a non-decreasing effect on the latent
probit-scale response.
}
\details{
The monotonicity constraint is increasing for every predictor. Reverse a
predictor before fitting if scientific knowledge requires its effect to be
decreasing. Missing or infinite values are not supported.

\code{nkeeptrain}, \code{nkeeptest}, \code{nkeeptestmean}, and \code{nkeeptreedraws} control
thinning independently. Each nonzero value must be no larger than, and
divide, \code{ndpost}.
}
\examples{
set.seed(42)
x <- cbind(exposure = runif(40), score = runif(40))
p <- pnorm(-1 + 1.5 * x[, "exposure"] + x[, "score"])
y <- rbinom(40, size = 1, prob = p)

fit <- probit_monbart(
  x[1:30, ], y[1:30],
  x.test = x[31:40, ],
  ntree = 10, ndpost = 20, nskip = 5,
  nkeeptreedraws = 0
)
fit
head(fit$prob.test.mean)

}
